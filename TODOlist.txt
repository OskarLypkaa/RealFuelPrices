Add continents to the number of countries. (?)

Add inflation rate date + AVG from fuel price (starting from the first date in%) Monthly.

Wrorking on DB conenction:
    - Adding method to check for empty values and a functionality to copy closest existing
    - Add functionality to check if date exist in DB (starting from most recent) and stops creating query. 

